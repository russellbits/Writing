06.web-agents.md

In addition to using agents to code, agents will do tasks for a myriad of reasons and users, including other agents. It then should be a goal that what coding agents are creating is then more legible (token-saving) to those agents. Coding agents on the web (as opposed to private applications) should push to create semantically meaningful web sites.